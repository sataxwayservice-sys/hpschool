{
    "name": "school/management-system",
    "description": "Complete School Students and Fees Management System",
    "type": "project",
    "version": "1.0.0",
    "require": {
        "php": ">=8.0",
        "kreait/firebase-php": "^7.0",
        "phpoffice/phpspreadsheet": "^1.29",
        "tecnickcom/tcpdf": "^6.6"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if (!file_exists('config/firebase-service-account.json')) { echo 'Please add your Firebase service account JSON to config/firebase-service-account.json'; }\""
        ]
    }
}
